// moved data to config for easy tweaking

export const weatherAppConfig ={
  MAX_CITY_COUNT : 8,
  CITY_EXISTS_MSG : 'City exists already',
}

export const weatherUnits ={
  TEMP_UNIT:' C',
  WIND_SPEED_UNIT:'ms',
  WIND_DIRECTION_UNIT:'deg',
}
